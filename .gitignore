# GATA Cloud Uploader - never publish these
tools/signing_key.json
tools/firmware_source.json
tools/autopsy_*.log
tools/gdbsrv_*.txt
tools/gdbsrv_out.txt
tools/gdbsrv_err.txt
tools/fresh_driver_test.log

# The cloud download channel is its OWN repository (Software/gata-firmware),
# nested here so publish_firmware.ps1 can write straight into it:
#   git -C firmware push
/firmware/

# Android build (the keystore is a SECRET: it signs the app - back it up,
# never publish it; dist/ holds generated packages)
android/gata-release.keystore
android/.gradle/
android/build/
android/app/build/
android/app/src/
android/gradle/
android/gradlew*
android/store_icon.png
android/manifest-checksum.txt
dist/
android/GataUpdater.cs.bak
